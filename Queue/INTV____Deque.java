package Queue;

import java.util.LinkedList;

import java.util.Deque;

public class INTV____Deque {
    /*
     * 
     * Many companies ask , deque and dequeue are same ...then its not dequque is removing elements from the quqeue and , deque is double ended queue
     * enqueue is adding elements into quque
     * 
     * 
     */

     public static void main(String[] args) {
        Deque <Integer> q = new LinkedList<>();

        q.addFirst(2);
        q.addLast(3);
        System.out.println(q);

        // Vector<Integer> v =  new Vector<>();
        // v.
     }

}
